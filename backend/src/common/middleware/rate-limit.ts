import {NextFunction,Request,Response} from "express"; type Opt={windowMs:number;max:number}; const buckets=new Map<string,{count:number;resetAt:number}>(); export function rateLimit(o:Opt){return(req:Request,res:Response,next:NextFunction)=>{const key=req.ip||'unknown'; const now=Date.now(); const b=buckets.get(key); if(!b||b.resetAt<now){buckets.set(key,{count:1,resetAt:now+o.windowMs}); return next();} b.count++; if(b.count>o.max) return res.status(429).json({error:'Too many requests'}); next();};}