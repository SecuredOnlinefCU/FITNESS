import {Router} from 'express'; import {z} from 'zod'; import {prisma} from '../../lib/prisma'; import {requireAuth,AuthenticatedRequest} from '../../common/middleware/auth'; import {asyncHandler} from '../../common/utils/async-handler'; export const feedRouter=Router(); feedRouter.use(requireAuth); feedRouter.get('/program/:programId',asyncHandler(async(req,res)=>res.json({items:await prisma.feedPost.findMany({where:{scopeType:'PROGRAM',scopeId:req.params.programId,status:'ACTIVE'},include:{media:true,_count:{select:{comments:true,reactions:true,saves:true}}},orderBy:{createdAt:'desc'},take:50})}))); feedRouter.post('/posts',asyncHandler(async(req:AuthenticatedRequest,res)=>{const b=z.object({scopeType:z.enum(['PROGRAM','COACH_PRIVATE','CHALLENGE']),scopeId:z.string(),bodyText:z.string().optional(),tag:z.string().optional(),media:z.array(z.any()).default([])}).parse(req.body); res.status(201).json(await prisma.feedPost.create({data:{authorUserId:req.user!.sub,scopeType:b.scopeType,scopeId:b.scopeId,bodyText:b.bodyText,tag:b.tag,media:{create:b.media.map((m:any)=>({...m,ownerUserId:req.user!.sub}))}},include:{media:true}}));})); feedRouter.post('/posts/:postId/comments',asyncHandler(async(req:AuthenticatedRequest,res)=>res.status(201).json(await prisma.feedComment.create({data:{postId:req.params.postId,authorUserId:req.user!.sub,bodyText:req.body.bodyText}})))); feedRouter.post('/posts/:postId/reactions',asyncHandler(async(req:AuthenticatedRequest,res)=>res.status(201).json(await prisma.feedReaction.upsert({where:{postId_userId:{postId:req.params.postId,userId:req.user!.sub}},update:{reactionType:req.body.reactionType||'LIKE'},create:{postId:req.params.postId,userId:req.user!.sub,reactionType:req.body.reactionType||'LIKE'}})))); feedRouter.post('/reports',asyncHandler(async(req:AuthenticatedRequest,res)=>res.status(201).json(await prisma.contentReport.create({data:{reporterUserId:req.user!.sub,...req.body,status:'OPEN'}}))));