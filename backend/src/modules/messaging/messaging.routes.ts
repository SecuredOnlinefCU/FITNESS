import {Router} from 'express'; import {prisma} from '../../lib/prisma'; import {requireAuth,AuthenticatedRequest} from '../../common/middleware/auth'; import {asyncHandler} from '../../common/utils/async-handler'; export const messagingRouter=Router(); messagingRouter.use(requireAuth); messagingRouter.get('/threads',asyncHandler(async(req:AuthenticatedRequest,res)=>res.json({items:await prisma.thread.findMany({where:{OR:[{coachUserId:req.user!.sub},{clientUserId:req.user!.sub}]},include:{messages:{take:1,orderBy:{createdAt:'desc'}}}})}))); messagingRouter.post('/threads',asyncHandler(async(req,res)=>res.status(201).json(await prisma.thread.upsert({where:{coachUserId_clientUserId:{coachUserId:req.body.coachUserId,clientUserId:req.body.clientUserId}},update:{},create:req.body})))); messagingRouter.post('/threads/:threadId/messages',asyncHandler(async(req:AuthenticatedRequest,res)=>res.status(201).json(await prisma.message.create({data:{threadId:req.params.threadId,senderUserId:req.user!.sub,messageType:'TEXT',...req.body}})))); messagingRouter.patch('/messages/:messageId/flags',asyncHandler(async(req:AuthenticatedRequest,res)=>res.json(await prisma.messageFlag.upsert({where:{messageId_userId:{messageId:req.params.messageId,userId:req.user!.sub}},update:req.body,create:{messageId:req.params.messageId,userId:req.user!.sub,...req.body}}))));