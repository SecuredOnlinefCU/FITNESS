{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter unified-fitness-backend-v1 prisma:generate && pnpm --filter unified-fitness-backend-v1 build"
  },
  "deploy": {
    "startCommand": "pnpm --filter unified-fitness-backend-v1 exec tsx src/main.ts",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "always"
  }
}
